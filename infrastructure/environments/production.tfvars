# Production Environment Configuration
environment = "production"
aws_region  = "us-east-1"
app_name    = "inked-draw"

# Production-specific settings would go here
# These values should be managed through GitHub Secrets or AWS Parameter Store
